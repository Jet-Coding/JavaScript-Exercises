/* Variables */
//Using Variable in output

//What does a variable do in JavaScript?
//Answer: It Store a value to use later.

//What Does console.log() do in javascript?
//Shows the stored value in output.

//Which line prints the value stored in city?
let city = "Paris";
console.log(city);

//Which line prints the number stored in score?
let score = 9;
console.log(score);

//Print the value stored in petName.
let petName = "Milo";
console.log(petName);

//Show the numbersaved in level, which is 4.
let level = 4;
console.log(level);

//What will this code print?
let drink = "water";
console.log(drink);

//What will this code print?
let year = 2025;
console.log(year);

//Order the line to store "blue" in color, then print it.
let color = "blue";
console.log(color);

//use the saved name to print what user holds.
let user = "Ana";
console.log(user);

//Fill in the command that shows points in the output.
let points = 15;
console.log(points);
